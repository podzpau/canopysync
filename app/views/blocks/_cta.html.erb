<div class="py-20 px-4 bg-gradient-to-r from-primary to-secondary">
  <div class="max-w-3xl mx-auto text-center">
    <h2 class="text-4xl font-bold mb-4 text-white">
      <%= config['heading'] || 'Ready to get started?' %>
    </h2>
    <p class="text-xl mb-8 text-white opacity-90">
      <%= config['text'] || 'Join thousands of satisfied customers today' %>
    </p>
    <%= link_to config['button_text'] || 'Get Started', config['button_url'] || '#',
        class: 'inline-block px-8 py-4 bg-white text-primary rounded-lg font-bold text-lg hover:bg-gray-100' %>
  </div>
</div>