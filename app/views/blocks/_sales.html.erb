<div class="py-16 px-4 bg-red-50">
  <div class="max-w-4xl mx-auto text-center">
    <h2 class="text-5xl font-bold mb-4 text-red-600">
      <%= config['heading'] || 'Sale!' %>
    </h2>
    <p class="text-3xl mb-8 text-red-700">
      <%= config['discount_text'] || 'Up to 50% Off' %>
    </p>
    <button class="px-8 py-4 bg-red-600 text-white rounded-lg font-bold text-lg hover:bg-red-700">
      <%= config['cta_text'] || 'Shop Sale' %>
    </button>
  </div>
</div>